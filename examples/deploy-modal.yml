# Example: the same trigger using the single `deploy-modal` JSON object instead of named inputs.

name: register-with-deploy-modal

on:
  workflow_dispatch:

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - name: Register LynxTrac release (deploy-modal)
        uses: LynxTrac/deploy-action@v1
        with:
          apikey: ${{ secrets.LYNXTRAC_API_KEY }}
          lynxserver: 'beta'
          deploy-modal: |
            {
              "blueprint": "web-prod",
              "version": "2.5.0",
              "artifacts": {
                "app-bundle": "https://your-ci/artifacts/app-2.5.0.zip"
              },
              "release": { "name": "Web 2.5.0", "description": "CI build" }
            }
